== #underline[Rules for Inequalities]
- *Trichotomy:* For any numbers $a$ and $b$, either $a < b$ or $a = b$ or $b < a$
- *Transitivity:* If $a < b$ and $b < c$, then $a < c$
- *Addition:* If $a < b$ and $c < d$, then $a + c < b + c$ and $a + c < b + d$
- *Multiplication:* If $a < b$ and $p$ is positive, then $p a < p b$,
  \ if $a < b$ and n is negative, then $n b < n a$
- *Reciprocal:* If $a$ and $b$ are positive numbers and $a < b$, then $1/b < 1/a$

== #underline[Triangle Inequality]
$ |a + b| <= |a| + |b| $

== #underline[A-G Inequality]
$ sqrt(a b) <= (a + b) / 2 \
#sub[equality holding if $a = b$] $

$ (a_1 a_2 ... a_n)^(1 / n) <= (a_1 + a_2 + ... + a_n) / 2 \ 
#sub[equality holding if $a_1 = a_2 = ... = a_n$] $
